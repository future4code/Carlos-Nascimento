
        {/*       <button onClick={() => getAllPlaylists()}>Ver todas as playlists</button>
         */}

        {/*       <input type="text" 
      name='pesquisar'
      placeholder="Pesquise uma playlist"
      value={playlistName}
      onChange={ (e) => { setPlaylistName(e.target.value) }}
      />
      
      <input type="text" 
      name='criar'
      placeholder="Insira o nome da nova Playlist"
      value={newPlaylist}
      onChange={ (e) => { setNewPlaylist(e.target.value) } }
      />
       <button type="submit" name="create" onClick={_ => createPlaylist()}>Criar Playlist</button>


      <form action="" name='novaFaixa'>
        <input type="text" 
        name='newTrackName'
        value={newTrack.name}
        placeholder="Nome da Faixa"
        onChange={ e => setNewTrack({name: e.target.value})}
        />
        <input type="text" 
        name='newTrackArtist'
        value={newTrack.artist}
        placeholder="Nome do Artista"
        onChange={ e => setNewTrack({artist: e.target.value})}
        />
        <input type="text" 
        name='newTrackURL'
        value={newTrack.url}
        placeholder="URL da música"
        onChange={ e => setNewTrack({url: e.target.value})}
        />
        <input type="text" 
        name='newTrackPlaylisID'
        value={playlistId}
        placeholder="ID da playlist"
        onChange={ e => setPlaylistId(e.target.value)}
        />
      <button type="submit"
      name="submitTrack"
      onClick={ _ => addNewTrackToPlaylist()}
      > Adicionar Faixa </button>
      </form>

      <input type="text" 
        name='deletePlaylist'
        value={playlistId}
        placeholder="ID da playlist"
        onChange={ e => setPlaylistId(e.target.value)}
        />
        <button type="submit"
      name="deletePlaylist"
      onClick={ _ => removePlaylist()}
      > Apagar Playlist </button>
 */}